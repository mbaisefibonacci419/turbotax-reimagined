interface DeadlineCardProps {
    deadline: string;
    extensionFiled?: boolean;
}
export default function DeadlineCard({ deadline, extensionFiled }: DeadlineCardProps): import("react").JSX.Element;
export {};
