import type { IRSFormTemplate } from '@nimbus/engine';
interface PdfFormViewerProps {
    template: IRSFormTemplate;
    instanceIndex: number;
}
export default function PdfFormViewer({ template, instanceIndex }: PdfFormViewerProps): import("react").JSX.Element;
export {};
