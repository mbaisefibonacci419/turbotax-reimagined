/**
 * Loading skeleton shown while the Forms Mode chunk (including Syncfusion
 * PDF Viewer + CSS) is being loaded via React.lazy / Suspense.
 */
export default function FormsSkeleton(): import("react").JSX.Element;
