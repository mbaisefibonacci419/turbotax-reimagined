import { ReactNode } from 'react';
interface WizardLayoutProps {
    children: ReactNode;
}
export default function WizardLayout({ children }: WizardLayoutProps): import("react").JSX.Element;
export {};
